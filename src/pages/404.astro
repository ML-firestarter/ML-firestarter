---
import NotFoundView from '../components/NotFoundView.astro';
import Base from '../layouts/Base.astro';
import { DEFAULT_LANG, ui } from '../lib/i18n.ts';
---

<Base lang={DEFAULT_LANG} title={ui[DEFAULT_LANG].notFound.title}>
  <NotFoundView lang={DEFAULT_LANG} />
</Base>
