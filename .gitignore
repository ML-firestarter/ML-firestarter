# dependencies and build output
node_modules/
dist/
.astro/

# other package managers' lockfiles: the site uses npm, and Netlify would switch to the manager of any of these
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# local tooling
.netlify/
.claude/
.DS_Store
.vscode/*
!.vscode/extensions.json

# exam questions, a clone of their private repository (see "Exams" in README.md)
exams/

# environment
.env
.env.*
!.env.example

# logs
npm-debug.log*
